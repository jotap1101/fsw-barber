services:
  db:
    image: postgres:latest
    container_name: fsw-barber-db
    restart: "no"
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - fsw_barber_db_data:/var/lib/postgresql/data

volumes:
  fsw_barber_db_data:
